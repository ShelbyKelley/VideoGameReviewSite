<%= form_with model: @game, multipart: true, local: true do |form| %>
  <% if game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(game.errors.count, "error") %> prohibited this game from being saved:</h2>

      <ul>
      <% game.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :game_title %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :game_description %>
  </div>

  <div class="field">
    <%= form.label :studio %>
    <%= form.text_field :studio, id: :game_studio %>
  </div>

  <div class="field">
    <%= form.label :esrb %>
    <%= form.text_field :esrb, id: :game_esrb %>
  </div>

  <div class="field">
    <%= form.label :image %>
    <%= form.file_field :image, id: :game_image %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
